# AUTO-GENERATED by generate_api_tests.py
# Manager handler tests are the behavioral baseline.

import json

from unittest.mock import (
    ANY,
    MagicMock,
    patch,
)

from v1.handlers.irc_census_report import (
    search_irc_census_reports_v1,
    get_irc_census_report_v1,
    list_irc_census_reports_v1,
)


# =============================================================================
# AUTHORIZATION TEST HELPER
# =============================================================================

AUTHORIZED_USER_ID = "HR - Special Use"


def authorized_request_context(request_id):
    """
    Build requestContext exactly as the Lambda authorizer sends it
    to the Main Lambda for the dedicated IRC Census PAT.
    """
    return {
        "requestId": request_id,
        "authorizer": {
            "lambda": {
                "userId": AUTHORIZED_USER_ID,
            }
        },
    }


# =============================================================================
# SEARCH - SUCCESS
# =============================================================================

@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportListResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "search_irc_census_reports"
)
def test_search_irc_census_reports_v1_success(
    mock_service,
    mock_outer_schema,
    mock_inner_schema,
    mock_context,
):
    mock_results = MagicMock()

    mock_results.items = [
        {
            "row_id": "1001",
            "last_first_name": "Doe, Jane",
        }
    ]

    mock_results.metadata.cursor = None
    mock_results.metadata.has_more = False
    mock_results.metadata.applied_filters = {}

    mock_results.metadata.model_dump.return_value = {
        "totalCount": 1,
        "cursor": None,
        "hasMore": False,
    }

    mock_service.return_value = mock_results

    mock_inner_schema.model_validate.return_value = MagicMock()

    outer = MagicMock()
    outer.model_dump.return_value = {
        "metadata": {
            "totalCount": 1,
            "cursor": None,
            "hasMore": False,
        },
        "data": [
            {
                "row_id": "1001",
            }
        ],
    }

    mock_outer_schema.return_value = outer

    event = {
        "pathParameters": {},
        "queryStringParameters": None,
        "requestContext": authorized_request_context(
            "test-search-success"
        ),
        "body": "{}",
        "isBase64Encoded": False,
    }

    response = search_irc_census_reports_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 200

    mock_service.assert_called_once_with(
        filters=ANY,
        sort=ANY,
        page=ANY,
        columns=ANY,
    )


# =============================================================================
# GET BY ID - SUCCESS
# =============================================================================

@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportDetailResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "get_irc_census_report_details"
)
def test_get_irc_census_report_v1_success(
    mock_service,
    mock_outer_schema,
    mock_inner_schema,
    mock_context,
):
    mock_results = MagicMock()

    mock_results.items = [
        {
            "row_id": "1001",
            "last_first_name": "Doe, Jane",
        }
    ]

    mock_results.metadata.cursor = None
    mock_results.metadata.has_more = False
    mock_results.metadata.applied_filters = {}

    mock_results.metadata.model_dump.return_value = {
        "totalCount": 1,
        "cursor": None,
        "hasMore": False,
    }

    mock_service.return_value = mock_results

    mock_inner_schema.model_validate.return_value = MagicMock()

    outer = MagicMock()
    outer.model_dump.return_value = {
        "metadata": {
            "totalCount": 1,
            "cursor": None,
            "hasMore": False,
        },
        "data": [
            {
                "row_id": "1001",
            }
        ],
    }

    mock_outer_schema.return_value = outer

    event = {
        "pathParameters": {
            "row_id": "1001",
        },
        "queryStringParameters": None,
        "requestContext": authorized_request_context(
            "test-detail-success"
        ),
    }

    response = get_irc_census_report_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 200

    mock_service.assert_called_once_with(
        row_id="1001",
        filters=ANY,
        limit=ANY,
        cursor=ANY,
        columns=ANY,
    )


# =============================================================================
# GET BY ID - MISSING ID
# =============================================================================

def test_get_irc_census_report_v1_missing_id(
    mock_context,
):
    event = {
        "pathParameters": {},
        "requestContext": authorized_request_context(
            "test-detail-missing"
        ),
    }

    response = get_irc_census_report_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 400

    body = (
        response["body"]
        if isinstance(
            response["body"],
            dict,
        )
        else json.loads(
            response["body"]
        )
    )

    assert (
        body["error"]["message"]
        == "IrcCensusReport ID is required."
    )


# =============================================================================
# LIST - SUCCESS
# =============================================================================

@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportListResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "search_irc_census_reports"
)
def test_list_irc_census_reports_v1_success(
    mock_service,
    mock_outer_schema,
    mock_inner_schema,
    mock_context,
):
    mock_results = MagicMock()

    mock_results.items = [
        {
            "row_id": "1001",
            "last_first_name": "Doe, Jane",
        }
    ]

    mock_results.metadata.cursor = None
    mock_results.metadata.has_more = False
    mock_results.metadata.applied_filters = {}

    mock_results.metadata.model_dump.return_value = {
        "totalCount": 1,
        "cursor": None,
        "hasMore": False,
    }

    mock_service.return_value = mock_results

    mock_inner_schema.model_validate.return_value = MagicMock()

    outer = MagicMock()
    outer.model_dump.return_value = {
        "metadata": {
            "totalCount": 1,
            "cursor": None,
            "hasMore": False,
        },
        "data": [
            {
                "row_id": "1001",
            }
        ],
    }

    mock_outer_schema.return_value = outer

    event = {
        "queryStringParameters": {
            "limit": "10",
        },
        "requestContext": authorized_request_context(
            "test-list-success"
        ),
    }

    response = list_irc_census_reports_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 200
    assert mock_service.called


# =============================================================================
# SEARCH - INVALID JSON
# =============================================================================

@patch(
    "v1.handlers.irc_census_report."
    "LambdaUtils.get_json_body"
)
def test_search_irc_census_reports_v1_invalid_json(
    mock_get_json_body,
    mock_context,
):
    mock_get_json_body.side_effect = json.JSONDecodeError(
        "Expecting value",
        "",
        0,
    )

    event = {
        "pathParameters": {},
        "queryStringParameters": None,
        "requestContext": authorized_request_context(
            "test-invalid-json"
        ),
        "body": "{invalid-json",
        "isBase64Encoded": False,
    }

    response = search_irc_census_reports_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 400


# =============================================================================
# GET BY ID - NOT FOUND
# =============================================================================

@patch(
    "v1.handlers.irc_census_report."
    "get_irc_census_report_details"
)
def test_get_irc_census_report_v1_not_found(
    mock_service,
    mock_context,
):
    mock_results = MagicMock()
    mock_results.items = []

    mock_service.return_value = mock_results

    event = {
        "pathParameters": {
            "row_id": "1001",
        },
        "queryStringParameters": None,
        "requestContext": authorized_request_context(
            "test-detail-not-found"
        ),
    }

    response = get_irc_census_report_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 404


# =============================================================================
# LIST - DEFAULT QUERY PARAMETERS
# =============================================================================

@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "V1IrcCensusReportListResponseModel"
)
@patch(
    "v1.handlers.irc_census_report."
    "search_irc_census_reports"
)
def test_list_irc_census_reports_v1_default_query_params(
    mock_service,
    mock_outer_schema,
    mock_inner_schema,
    mock_context,
):
    mock_results = MagicMock()

    mock_results.items = [
        {
            "row_id": "1001",
            "last_first_name": "Doe, Jane",
        }
    ]

    mock_results.metadata.cursor = None
    mock_results.metadata.has_more = False
    mock_results.metadata.applied_filters = {}

    mock_results.metadata.model_dump.return_value = {
        "totalCount": 1,
        "cursor": None,
        "hasMore": False,
    }

    mock_service.return_value = mock_results

    mock_inner_schema.model_validate.return_value = MagicMock()

    outer = MagicMock()
    outer.model_dump.return_value = {
        "metadata": {
            "totalCount": 1,
            "cursor": None,
            "hasMore": False,
        },
        "data": [
            {
                "row_id": "1001",
            }
        ],
    }

    mock_outer_schema.return_value = outer

    event = {
        "queryStringParameters": None,
        "requestContext": authorized_request_context(
            "test-list-defaults"
        ),
    }

    response = list_irc_census_reports_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 200
    assert mock_service.called


# =============================================================================
# AUTHORIZATION - WRONG USER MUST GET 403
# =============================================================================

def test_irc_census_report_forbidden_for_other_user(
    mock_context,
):
    event = {
        "queryStringParameters": None,
        "requestContext": {
            "requestId": "test-forbidden-user",
            "authorizer": {
                "lambda": {
                    "userId": "some-other-user",
                }
            },
        },
    }

    response = list_irc_census_reports_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 403


# =============================================================================
# AUTHORIZATION - NO AUTHORIZER MUST GET 403
# =============================================================================

def test_irc_census_report_forbidden_without_authorizer(
    mock_context,
):
    event = {
        "queryStringParameters": None,
        "requestContext": {
            "requestId": "test-no-authorizer",
        },
    }

    response = list_irc_census_reports_v1(
        event,
        mock_context,
    )

    assert response["statusCode"] == 403
